import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings")
                .font(.title)
            // 在这里添加更多设置选项
        }
        .padding()
    }
}
